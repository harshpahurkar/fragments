# fragments

This is the Fragments backend (Express) used in the CCP554 workspace.

Run locally (development):

```powershell
cd fragments
npm install
npm run dev
```

Run tests:

```powershell
npm test
```

Production start:

```powershell
npm start
```
